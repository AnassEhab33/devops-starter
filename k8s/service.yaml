apiVersion: v1
kind: Service
metadata:
  name: devops-starter-service
spec:
  type: NodePort # Expose outside cluster
  selector:
    app: devops-starter
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001 # Access on this port
